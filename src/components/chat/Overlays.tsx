// WAVE-1-STUB — Wave 1 Agent C replaces this with the real Overlays
// (QuickJump ⌃Q, TreeMap ⌃T, Shortcuts ⌃?). Consumer is ChatApp (Agent B).

export interface OverlaysProps {
  chatId: string;
  currentNodeId: string;
}

export function Overlays(_props: OverlaysProps) {
  return null;
}

export default Overlays;
