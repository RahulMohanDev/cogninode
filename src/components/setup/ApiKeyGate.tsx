// WAVE-1-STUB — Wave 1 Agent A replaces this wholesale with the real key gate.
import type { ReactNode } from "react";

export function ApiKeyGate({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

export default ApiKeyGate;
